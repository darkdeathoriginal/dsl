// app/(units)/layout.tsx
import { ReactNode } from 'react';

export default function UnitsGroupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>; // Just passes children through
}